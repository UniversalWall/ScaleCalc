# Changelog

本文件记录 `com.wayward.scalecalc` 的所有值得注意的变更；版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)，格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
版本号与 `package.json` 的 `version` 必须一致；发布以 git tag 标记（本作品：`scalecalc/v0.4.0`）。

> ℹ️ 历史条目里的 `W-*` / `S-*` / `PD-*` / `PK*` / `E-*` / `LD-*` 等编号是**开发期的内部工作项与错误登记号**，
> 保留下来是为了可追溯（它们**不指向**本仓库里的文件，不影响照抄本文的命令与路径）。
>
> ℹ️ **`0.4.0` 之前的条目用的是旧名**（`com.ugams.scalecalc` / `UGams.*` 命名空间 / `UGams/` 菜单前缀）——
> 那是当时的真实状态，**故意保持原文不改**，改名一事的完整说明见 `0.4.0` 段。

## [Unreleased]

_暂无。_

## [0.4.0] - 2026-09-22

> 🔴 **改名版**：`ugams` 是立项时随手起的占位名，本版起统一为 `wayward`。
> 这是**破坏性变更**（0.x 阶段允许，但仍按规矩逐条列明）。

### 变更

- **包名**：`com.ugams.scalecalc` → **`com.wayward.scalecalc`**。装过旧版的工程要把 `Packages/manifest.json`
  里的依赖键一起改掉，否则会解析失败或出现"两个包"。
- **四个程序集与命名空间**：`UGams.ScaleCalc[.Unity|.Editor|.Tests]` → **`Wayward.ScaleCalc[.Unity|.Editor|.Tests]`**。
  `.asmdef` 文件与其 `.meta` **成对改名、GUID 未动** ⇒ 场景与预制体的组件引用不会断。
  **症状**：用旧命名空间 `using` 的地方报 CS0246；**根因**：根命名空间整体更换；**回退**：改回 `using UGams.ScaleCalc;`。
- **编辑器菜单**：`UGams/ScaleCalc/打开选型台` → **`Wayward/ScaleCalc/打开选型台`**。
- 🔴 **`EditorPrefs` 键前缀**：`UGams.ScaleCalc.ScreenTiers.*` → `Wayward.ScaleCalc.ScreenTiers.*`。
  **症状**：升级后打开选型台，之前存过的屏幕档位工作集读不到，回落到内置初值；
  **根因**：键名带包名前缀（这是刻意的，防不同作品互相覆盖）；**回退**：旧键仍在注册表里，改回旧名即可读回。
- **源码里的包路径字面量**（`Packages/<包名>/...`）同步更新；包内测试的命名空间白名单与禁词表同步更新。
- **`README` 安装地址**、**屏幕档位清单文件首行注释**、**证据导出 CSV 表头**里的包名同步更新。

### 修复

- **改名残留收口**（都不影响功能，但会让读源码或读样本的人看到两套名字）：证据册 8 份的「来源行」·
  `Samples~` 四个场景里缓存的 `m_EditorClassIdentifier`（只是缓存标识，真正的引用走 `m_Script` 的 GUID，
  改名时 GUID 保号 ⇒ 引用从未断过）· `StarterKit导入方案` 里那行「暂用 `UGams`」· 平台状态册里停在 `v0.3.0` 的作品总览行。
  ⚠️ **证据册正文里的旧包路径与旧类名、以及各 `CHANGELOG` 中 `0.4.0` 之前的条目**，按留痕原则**故意不改** —— 那是当时的真值。

### 文档

- **`README` 开头换成产品定位文案**：一句定义（基于纯 C# 实现的 UGUI 多设备适配量化分析工具）加三条客观数据支持
  （多设备数据推演 · 安全设计区计算 · 明确边界）。标题、英文摘要与四节结构不变。

## [0.3.0] - 2026-09-22

> 本版是**首个公开开源版本**（仓库 <https://github.com/UniversalWall/GITHUB>，作品在 `Works/ScaleCalc/`）。
> 相对 `0.2.0`：选型表新增「对账」状态列与「裁留图」逐档示意图两列 · 推荐行由两件补全为**五件** ·
> 术语统一为全称「安全设计区」· 撤除「更多列」开关与窗口底部四行文本 · 修掉"末列整列画不出来"等一批缺陷 ·
> README 补一节**用安全设计区做多分辨率适配的实操**并做开源化重写。

### 新增

- **对账状态列**（表格**第 1 列**）：`失` / `过` / `—` **三态**，
  🔴 **只有"当前渲染尺寸"那一行**可能判出 `过`/`失`（真值只对它成立），
  其余行如实写 `—`；**鼠标悬停**给原因 —— `—` 的**四种成因**（本次会话没对过账 / 对过但未取到 /
  **条件与本表不同** / 尺寸不在档位表里）**四句互不相同**，其中"条件不符"排最前（它最容易被误读成"没对过账"）。
  口径与文案收在纯函数 `ReconcileVerdict`（`ScaleTableColumns.ReconcileTitle = "对账"`），**可在 EditMode 逐字断言**。
- **整表真值语境** `ReconcileContext`：把"这一张表为什么有/没有真值"随行带出（原来只活在底部那一行文本里）。
- **逐档裁留示意图列**（`裁留图`，表格**第 2 列**）：
  每行一个小图 —— **参考画布框 + 本档画布框**，两框**同一比例尺**、都落在**信封**（两框的逐轴并集）里
  ⇒ **比例真实、且永不出血到相邻行**。两框之差画成**色带**：**红 = 被裁**（参考有、画布没有）·
  **黄 = 留白**（画布比参考大）⇒ **一格里能同时看见裁与留**；
  两框一样大时**一条带都不画**（"正好"就是没有差）。🔴 **与结论区那张跨档图不同词、不同义**（那张的内框是「安全设计区」）：
  本图的画布框是**这一档自己的画布**，文案点名「本档画布」且**不许**出现「安全设计区」。
  几何是纯函数 `ScaleFit.DiagramColumnOf`（信封坐标 + 四条带，**逐位可断言**）；
  悬停给红黄含义 + 两轴读数 + 限定语。列宽 `52~60`（作画区随行高拟合：竖屏 9:16 = **22×22**、超宽 21:9 = **48×20**）。
  🔴 **同日可读性优化（原地，不换表达）**：画布改**实心蓝块**、参考框留 **1px 灰描边**（两块浅描边在 22px 里
  会糊成一团）· 图层顺序改成"**块 → 带 → 参考框**"（"留白"的黄段落在画布块**之内**，必须压在实心块上面才看得见）·
  差带加 **1px 地板**（差 0.6% 时按比例只有 0.1px，等于看不见；真实量仍在悬停里）· **读图约定进图例**
  （`灰框=参考画布 · 蓝块=本档画布 · 红=被裁 · 黄=留白`，实测 74 字符 = 761px ⇒ 最小窗口不换行）。
  🔴 **并整体放大**（用户「图再大一点」）：行高上限 `26 → 30`（`ScaleTableRowHeight.Max`，下限 20 不变）·
  呼吸位 `2 → 1` · 作画区宽度上限 `48 → 52` ⇒ 竖屏档位 **22×22 → 28×28**、16:9 **39.1×22 → 49.3×28**
  （线性 **+27%** / 面积 **+62%**）；⚠️ 代价是同高度表格里可见行数少约 1 行（本次实测窗口 8 条目仍全可见）。
  ⚠️ 同日试过的「**两轴一维量值条**」**被用户否掉**（"不是搞这个横条抽象"，已回退 `b8df7d5`）。
  **新增 11 条断言**（几何 6 + 列定义/文案 5）。

### 变更

- **选型台推荐行补全**：结论区那一行 `推荐 match …` 由「最坏裁切 + 留白代价」**两件扩到五件** —— 新增 **目标函数**（`目标：最坏裁切最小`）、**会裁档数**（`会裁 6/7`）与**安全设计区 `W×H`**，字段顺序与三方式小结逐字对齐。
- **术语统一**：三方式小结里的「安全区」统一为全称「**安全设计区**」（此前有一条断言把简称钉住了）。
- 新增 **5 条断言**（四件与小结同源 · 反例「会裁 6/7」 · 永不编数 · 术语全称 · 目标文案）；双基线 **245 / 243 → 250 / 248**。
- **列集 8 → 9**：行首插入「对账」列 ⇒ 前四列预算 `450 → 364`（**更宽松**）；
  `ScaleFit.SortKeyOfColumn` 的映射整体 +1（`0` 归 `None`：状态列没有排序语义，点列头只高亮、不重排）；
  「导出当前视图」的列数文案改为**读 `All.Length`**（以后加列不用再改它）。

### 移除

- **撤除「更多列」开关整条链路**（用户 2026-09-21 裁定"没必要"）：工具栏第三个开关 + `Extra` 三列诊断
  （`refPPU` / `夹取诊断` / `分支`）+ `VisibleColumns(bool)` + 相关断言。
  依据：实测那三列里 `分支` **整表同值**（本窗口 `ScaleMode`/`RenderMode` 恒定）、`夹取诊断` 对内置 7 档**恒为"无"**
  ⇒ 只有 `refPPU` 一列逐档有差异。⚠️ `ComputeWidths`/`Distribute` 的**列集可传**能力**保留**（它是公开 API 的形参，与"更多列"无关）。
- **撤除窗口底部的分支栏与总结行**（用户同日裁定"这些文本详情没必要"）：`.uxml` 两个节点 + `.uss` 七条规则 +
  `UpdateBranchStatus` / `UpdateSummary` / `CountsText` / `TruthStatusText` 四个方法。
  实测释放 **≈128px**（顺带治掉一处真实溢出：删之前 `summary` 底边 **981 > 窗口高 961**）⇒ `table-host` **224 → 360px**、可见行 **8 → 12**。
  ⚠️ **如实登记的损失**：分支可视化（一眼看出踩了早退）与界面上的兜底链诊断**不再成立**；
  补偿出口 = 对账状态列的悬停（真值来历/成因）· 证据包 CSV（6 列兜底诊断）· README。
- **撤除「选中行详情」面板**（用户裁定「这个功能不要了」）：`.uxml` 的 `row-detail` 节点 +
  `.Verdict.uss` 的 `.row-detail` 规则 + `Window/ScaleCalcWindow.Detail.cs` + `Fit/ScaleFit.Detail.cs`
  + 那条 `DetailText` 断言**成对一起删**（节点/规则/纯函数/断言）。**行高亮保留**（列表自身行为），
  选中身份（`m_SelectedTierName`）搬进新章节 `Window/ScaleCalcWindow.Selection.cs`。
  实测：`table-host` **345 → 381px**（释放 ≈36px）⇒ 主表可见 **11 行**（8 条目一次看全）、同层无重叠。

### 修复

- 🔴 **末列「裁留图」整列画不出来 + Console 刷 `InvalidCastException`**（2026-09-21 用户现场报错，`E-36`）：
  根因是**分派判据用错了对象** —— `bindCell` 与 `ClearCell` 都按 **`element.childCount`** 分派，而同日「方案 A」
  把图格从 **3** 个子元素减到 **2**（去掉格内文字）⇒ 图格分支**永不命中**，元素掉到普通格的 `((Label)element)`
  强转（`ScaleCalcTableBinder.cs:155`）⇒ **每绑一格抛一次**，异常沿 `VisualTreeLayoutUpdater` 冒出去 ⇒
  **整个布局回合中止**（表象是"图没画出来"/"内框停在 `0×0`"，两者是**同一个根因**）。
  修法：**判据只用"声明"** —— `bindCell` 按 `spec.Kind` 分派 · `ClearCell` 收 `kind` 参数 · 普通格用 `is Label`
  而非强转 ⇒ **绑定与清格路径一律不许抛**（抛一次 = 打断整轮排布，不是丢一格）。
  🔴 **机械化**：`ScaleCalcTableBinder` 由 `internal` 改 **`public`**（同包先例 `ScaleCalcTierBinder`），
  新增 `Tests/EditMode/Table/ScaleCalcTableCellContractTests.cs`（**4 条**：**逐列 × 逐行**真调
  `makeCell()`→`bindCell()`→`unbindCell()` · 越界索引安全清格 · 图格几何+悬停+复位 · 普通/底衬格各写自己的子元素）
  —— 此前**没有任何判据碰过 `bindCell`**（全是读源码字符串），这类事故才能一路绿着上线。双基线 **263 / 261 → 267 / 265**（失败 0）。

### 文档

- **README 精简**（**430 → 62 行**）：只做告知 —— 这东西解决什么问题、怎么装、怎么打开、边界在哪；
  说明性细节（安全设计区的完整操作步骤、逐条已知限制、换算链公式、控件级说明）不再堆在 README 里，
  移到开发仓库的《设计要点》与《实现文档》，README 只留入口。「与我有关」只留 MIT 许可证一行。
  🔴 **`Q-15`：README 必备节由五节改为四节** —— 「边界声明」不再要求写在 README（改由《设计要点》承担），
  包内断言同步改为「四节 + 许可证 + 包名」。
- **`package.json`**：`version` `0.2.0 → 0.3.0` · 补 `documentationUrl` / `changelogUrl` / `licensesUrl`
  （《包与发布规范》§一 的「推荐」档）· `author` 改为 `{ "name": "UniversalWall", "url": "https://github.com/UniversalWall" }`。
- **`LICENSE`**：版权行 `UGams → UniversalWall`（与 `package.json` 的 `author` 保持一致）。
- **本 CHANGELOG**：`[Unreleased]` 收成 `[0.3.0]`（下方各节即本版内容）· 顶部补一行「内部编号」说明。

## [0.2.0] - 2026-09-20

> 本次发布 = 两块**用户可感的新功能**（《选型台信息呈现》· **档位清单导入 / 导出**）+ 一批修复与内部整理。
> **无破坏性变更**：公开 API 只增不减（`EvidenceExporter.Export` 加的是**默认值不变**的可选参数）；界面上的物理覆盖表已在早前重做中撤除，见「移除」。

### 新增

- **档位清单导入 / 导出**：档位管理区多了**两个按钮**（与「新增一行」**同一行**——
  另起一行会多占 ≈23px，把主表从「一次看全」挤掉一行）。
  - 「**导出档位清单…**」：把当前工作集导成一份**可入版本库、可手工编辑**的 `.txt`（**UTF-8 无 BOM + LF**，默认名 `ScaleCalc-屏幕档位清单.txt`）。
    文件 = 一段 `#` 说明头 + **就是 `EditorPrefs` 存的那份 `v1` 载荷**（一行一档、制表符分列）⇒ **没有新格式、零迁移**；
    剥掉说明头之后与载荷**逐字节相同**（同源可断言，导出两次也逐字节相同）。
  - 「**导入档位清单…**」：**整体替换**当前工作集（内置档被改过的名字/尺寸**照收**），**先解析、全过才动数据** ⇒ 坏文件零副作用。
    四道闸：只剩注释 · **数据部分**超 8192 字符 · 载荷层那 7 条封闭理由 · **标识重复**（报两行行号）。
    失败消息里的行号是**文件物理行号**（含说明头）并附**该行原文**；成功文案报**替换前后的档数**与参与计算档数。
  - 宽容口径：导入对 **BOM / CRLF** 都宽容；导出**恒 LF、无 BOM**（多出的 3 个字节会被 git 当内容差异）。
  - 🔴 **不碰场景**：导入导出一个场景操作都没有（有行为层断言守：成功与失败两条路径各跑一次真实 IO 往返，场景集合逐项不变）。
  - 双基线 **226 / 224 → 243 / 241**（**+17** = 本功能本体 **+16** + 收口后补的「接线断言」**+1**）。
- **表格口径图例**：选型表**正上方**多一行说明——`灰字 = 内核离线算的（未经引擎对账）；真值只对「现场对账」测过的那一行成立`。
  来由：要求"**不许假装整张表都对过账**"，而此前这份语义**只活在单元格的灰字样式里**，表头一声不吭 ⇒ 读者第一眼会以为每列都对过账。
  文案是纯函数 `ScaleFit.TableLegend()`（窗口只读它 ⇒ 逐字可断言）。🔴 **列头不逐个加"（内核）"后缀**：离线语义不属于某一列（横向/纵向/实际画布/sf 都是内核算的），逐个加会把 5 个表头一起拉长、挤掉列宽。
- **安全区示意图**（C 批，设计册 §6.3）：结论区**右侧**一张 200×113 的小图——**外框 = 参考画布**、
  **内框 = 安全设计区**（居中）、**四条边带 = 危险带**（红 = 至少一档会裁 / 黄 = 全都不裁）。
  实测：内框 101×98（= 参考的 51%×87%，与结论条同一组数）、左带 49px。
  🔴 放在**右侧**是刻意的：它的高度被左边的文本块吸收 ⇒ **不用抬窗口底线**（`E-25` 的账要算平）。
- **单元格比例底衬**（C 批，§6.2）：`横向`/`纵向` 两列的数值下面多一层色块——**宽度 = 画布 ÷ 参考**
  （红 = 裁 / 黄 = 留 / 绿 = 正好，与文字前缀 **双编码**）。它是**绝对定位的底衬**、不是"文字 + 条"并排：
  行高只有 20~26px、列宽 130~240px，并排必然挤掉数值；底衬**不占额外空间**，数值照旧逐字可读。
- **选中行详情**（C 批，§6.4）：点选型表任一行 ⇒ 表下方两行摊开它的换算细节
  （`sf` / `refPPU` / 实际画布 / 分支 / 三处夹取 / dpi 有没有走兜底 / 真值三项差值或"未经引擎对账"）。
  🔴 **选中态按"档位名"记**：排序、筛选、改参数都会重建整张表（实测 `Rebuild()` 后 `selectedIndex` 归零），
  所以重建后是**按身份**把选中放回去的——不会出现"详情显示的是上一次点的那一行"。
- **「更多列」开关**（C 批，§7.3 的分级）：默认仍是 **8 列**；勾上追加三列**逐档真的有差异**的诊断——
  `refPPU` / `夹取诊断`（谁被夹过，没夹就写"无"）/ `分支`（早退分支一眼可见）。
- **字符条**（C 批，设计册 §6.1）：结论区多一块**纯字符串**画的比例条——`手机竖屏超长 9:19.5  ├████████········┤  裁 −49.0%`。
  读法：**短 = 裁**、**越过 `┊` = 留**（`┊` 标的是"参考到此为止"）。只画**最需要注意的 3 档**（精确读数看数值列；
  一行 ≈18px，画满 39 档要 700px——高度预算是硬约束）。
- **「导出当前视图（所见 8 列）」**（C 批，§6.5）：把**你现在看到的这张表**导成 CSV——界面上的 8 列、**当前的排序与筛选**都在里面
  （既有「导出当前表」是 `EvidenceExporter` 的 15 列固定台账，两者不是替代关系）；UTF-8 带 BOM、走保存对话框。
- 🔴 **选型台能"看到结论"了**：  - **匹配方式三选一**（`Match` / `Expand` / `Shrink`）：换方式整表重算；`Expand`/`Shrink` 不读 `match` ⇒ 滑杆**灰掉 + 同屏说明**。
    实测：`Expand` 把"会裁档位"从 **6/7 变成 0/7** 且安全设计区 = **整张参考画布**（代价是最坏留白 +285%）；`Shrink` 永不留白但裁得最狠。
  - **顶部结论条**（常显）：`参与 N 档 · M 档会裁 · 最坏：<档位> <横向|纵向> −x.x%（左右各 −NNNpx）· 安全设计区 W×H（参考的 pp%×qq%）· 结论只对当前勾选的 N 档负责`。
  - **三方式对比小结**（标题逐字带「三选一」）+ **`match` 推荐值**（连留白代价一起报；内置 7 档、参考 1920×1080 下推荐 `match 0.17`：最坏裁切 49.0% → **21.2%**，留白代价 **+206.3%**）。
  - **「复制结论」**与**「导出设计约束单（几何口径）」**两个按钮：后者落一个四行 `.txt`（文件头限定语 / 结论行 / 安全设计区 / 危险带（左右各 / 上下各）），UTF-8 带 BOM，给美术/策划用。
  - 选型表 **5 → 8 列**：`屏幕档位 · 比例 · 横向 · 纵向 · 实际画布 · sf（1 画布单位 = ? px）· 屏幕尺寸 · 差值`——决定性列在前，
    最小窗口下不滚动就能读到"会不会裁"；「同比例」标记并进**比例**列、「方向对」是同列后缀 `⇄`（不另立列）。
  - **比例列由像素算**（最小整数比）：内置两档的名字与像素比不一致（`超宽 21:9` 实为 **64:27**、`手机竖屏超长 9:19.5` 实为 **6:13**）——
    名字可以被改，比例列**不读名字**。
- **真值条件现在带"匹配方式"**：`LiveTruth` 记下测量时的 `ScreenMatchMode`，切到 `Expand`/`Shrink` 后
  **不会**再拿 `Match` 下测的真值去比差（那会造出假差值）——条件不符时整表如实标"离线列"并说明测量条件。
- **危险带常显**：结论条下面多一行 `危险带（左右各 / 上下各）：左右各 −471px · 上下各 −72px`
  （参考相对**安全设计区**被裁掉的边缘宽度，**单侧**）——"要留多少边"这个数因此不用导出也能直接读。
- **排序 / 筛选 / 一键排除会裁档位**：点列头排序（**默认不排序**；裁切量列首点"最坏在前"）·
  工具栏两个筛选开关（**只看会裁** / **只看竖屏**，默认全关）· 按钮「排除会裁档位」把"我不可能为它单独排一版"一键落地。
  🔴 **排序与筛选只改"看的顺序"**：工作集、结论条、安全设计区、导出**都不变**（筛选后表里可能只剩 3 行，结论条仍按勾选的工作集说话）。
- **新断言 27 条**（批 A1+A2，EditMode 总数 **164 → 191**）：逐档几何/文本 · 跨档统计与结论条 · 列定义 · 真值条件 · 结论区界面；
  **批 B 再 +10**（**191 → 201**）：排序/筛选的纯函数 · 危险带与结论条同源 · 排除动作的顺序纪律。

- **屏幕档位现在可以改名**（2026-09-20 用户提出）：管理表的「屏幕档位」列从只读变成**可编辑**（回车 / 点开别处才提交），
  **内置档与自定义行都能改**。改名的三项承诺：① 改完**还是同一行**（勾选态不丢）；② **内置档的来源标注不受影响**
  （来源按档位的内部标识取清单原文，与显示名无关）；③ 证据包里照旧如实标注来源。
  - **把名字清空**：自定义行会**回到自动名**（如 `自定义 1600x900`）并如实提示；内置档的名字**不能清空**
    （当场回滚并说明"名字未改动"）。
  - 名字超 32 字会截断，并**如实**告诉你截成了什么（与前几版同口径）。

- **兜底链诊断**（`W-ScaleCalc-10` 重做）：选型台**总结行最后一行**常显一句话，例如
  `兜底链：本机读数 144（编辑器读数不可信）；若报 0 则走兜底 96 ⇒ scaleFactor 1.33333337`。
  - **两种来源必须分得清**：`本机实测`（你这台真报了多少）/ `兜底常量`（一旦不报会退化成多少）。
    **本机读数读不到（`Screen.dpi <= 0`）时只出后者**——那时两行的输入都是 `0`，摆两行只是重复。
  - 机器可读版：`EvidenceExporter.BuildFallbackCsv(...)`（6 列 × **1~2 行**，行数随读数是否可得）。
  - ⚠️ 这句话给的是**两个情形**，不是**两个结论**：本机那侧的"没走兜底"只意味着"**假如**引擎读到这个数"，
    **不代表你这台以后不会退化**。
  - 新增断言 **A-12 / A-12′** 钉住"两态可区分"与"缺位不凑条"，并保住平台验收线 `B-4`
    要的「打印走了哪个常量 + 对应 `scaleFactor`」。

- **屏幕档位可维护化**：选型台从"固定 7 档"变成**你自己维护的工作集**。
  - **档位管理表**（新，在上、默认展开）：逐行**勾选/取消**（这一档参不参与计算）· **删除**自定义行 ·
    「**新增一行**」· 在「屏幕尺寸」格里**就地编辑**（回车/失焦提交）。**内置 7 档不可删**（点了给可读说明，
    **不是**禁用按钮——禁用的按钮不触发悬停提示，使用者就问不出"为什么点不动"）。
  - **选型表收窄**：砍掉 `ConstantPixelSize` / `ConstantPhysicalSize` 两列（它们**按构造恒定**，摆在一起
    会让人以为三列在做行级对比）。**列 7 → 5**；那两列的数字没消失——模型字段与"导出当前表"照旧含它们。
  - ~~**物理覆盖表**（新，在最下、**默认折叠**）：40 行「物理单位 × dpi 档位」，把"真报 / 兜底"
    两条路并排摊开。这张表**不做增删勾选**。~~
    🔴 **本版内即被撤除**——见上面「移除」段。
    **兜底链的可视化没有丢**：改由总结行的一句诊断承担。
  - **持久化**：工作集按**工程**存进编辑器配置（键带工程路径短哈希 ⇒ 同机不同工程互不覆盖），
    **不落你的仓库、不进版本库**；内容损坏 / 畸形 / 越界一律**整份回退到内置 7 档并如实报告**，不抛异常。
  - 状态栏把三个数**分开**写：`档位 N 行`（内置表规模，**不随勾选变**）· `参与计算 M 档` · `K 模式`（读列定义）；
    操作反馈另起一行，**不再覆盖**总结行。
  - 🔴 **证据包与工作集解耦**：导出内容**不受勾选影响**，仍是内置 7 档全集——这是"留仓库的证据
    逐字节可复现"的前提。文件头、状态栏、README 三处都注明。

- **选型台内直接导出换算证据包**：窗口新增「导出换算证据包（≥5 组）」按钮（与「导出当前表（CSV…）」并排，**两者都走保存对话框**，不做路径输入框）。默认目录 = 工程内约定落点，默认文件名 `ScaleCalc-005-换算证据包.csv`；取消对话框什么都不做，写出失败把可读原因写进状态栏。**撤除菜单项** `UGams/ScaleCalc/导出换算证据包（≥5 组）`（有窗口按钮之后它没用了；脚本侧改走 `ExportTo(path)`，须显式给落点）。
- **清场不变量断言**（10 条）：宿主对象一个不删 · 脏宿主跑完一比一复原 · 场景集合与活动场景不变 ·
  未命名旁观场景仍在且内容不变 + 诊断点名 · 未保存宿主 ⇒ 拒绝且零副作用；另有 5 条**源码级守卫**
  （实现里不许出现对象级销毁、不许用软标识判归属、只关自己建的场景；`SetActiveScene` 在**协议实现里一处都没有**——
  实测 2 处都在测试夹具；协议根本不改活动场景）。

### 修复

- **档位清单：注释只在"文件开头"才被跳过 → 改成全文过滤**：此前剥注释**只剥开头连续段**，
  于是往数据行之后补一行 `# 说明` 会被判「第 N 行字段数不对」——而注释头里明明写着"以 # 开头的行只是说明，导入时会被跳过"。
  现在**任意位置**的 `#` 行与空行都不进数据部分（数据行行首永远是标识 `builtin:` / `c:`，永不含 `#` ⇒ 不会误剥）。
  随之把「载荷内行号 → 文件物理行号」做成**对照表**（滤掉的行会让偏移不再固定），报错才指得准。
- **档位清单：载荷里若有重复标识，工作集会静默少一档**：`TryDecode` 不查"标识唯一"，
  而 `ScreenTierSet` 的构造**按 `Id` 去重、只留第一条** ⇒ 以为 9 档、实际 8 档且**毫无提示**。
  现在 `LoadTiers` 在解码成功之后补一道检查：命中即**整份回到内置 7 档**并在状态栏说明原因
  （`有重复的档位标识「…」（照常载入会静默少一档）`）。`TryDecode` 的 **7 条封闭理由一个字没动**。
- **差值列会显示「Δsf=0.00」却同时报「对账失败」**：差值原来只用两位小数，而判"对不对得上"的容差是 **0.001**
  ⇒ `|Δ|` 落在 0.001~0.005 时**判失败、显示 0.00**，看表的人只会觉得"差 0 却在报警"。
  差值这条线（界面差值列 / 状态栏那句）统一改成 **4 位小数**（`AxisVerdict.FormatDelta`）。
  ⚠️ **证据包那条链路一个字没动**（它有自己的逐字节基线）。
- **对账配色挂在"给人看的文案"上**：判定状态原来用 `verdict.StartsWith("对账失败")` 决定加哪个样式类
  ⇒ 文案改一个字，配色就**静默失效**（编译不报错）。改成直接认 `mismatch` 这个 bool。
- **两份证据 CSV 的列定义收成一处**：新增 `EvidenceColumns` 作为**单一真相源**——
  「当前表」导出（15 列）与「换算证据包」（16 列）**共用**内核三列与行尾七列（真值三列 / 差值三列 / 输入源），
  各自只声明自己多出来的列。**产物逐字节未变**（逐字节基线仍过，仓库里那份证据包不需要重新生成）。
  两份列数**仍然不同**，这是有意的分工，不是要合并。
- **一条测试守卫其实什么都没守**：`TestFixtureNeverReplacesTheWholeSceneSet`（守"夹具不许用 `NewSceneMode.Single`"）
  用 **不递归** 的 `Directory.GetFiles` 扫 `Tests/EditMode`，而目录整顿后这一层**已经一个 `.cs` 都没有**（顶层 0 个 / 递归 43 个）
  ⇒ 它扫的是空集，**保护力为零**（当时确实没有违规，但真出现违规也拦不住）。已改成递归，并**把"扫到几个文件"一起断言**——
  从此"扫空目录"本身就是失败，不会再静默变成假绿。
- **文案把推断写成事实**："非主 Display：**引擎改用** `Display` 的渲染尺寸"改为"与引擎是否同源**未验证**"。
  代码注释本来就是中性表述；这条分支本机（单 Display）**仍未实测**，标注继续留在 README「已知限制」第 6 条。
- **档位管理表只显示 7 行，加到 9 档就看不全**（2026-09-20 用户提出）：`tier-host` 原来把 `min` 与 `max` **都写死 200px**
  （= 表头 + 内置 7 行 + 一行余量）。现在高度**跟着条目数长**（`ScreenTierColumns.HostHeightFor`，纯函数），
  并夹在 `[200px, 可用高度 × 30%]`——上限是为了不把选型表与底部各段顶出窗口；超出上限仍由列表自己滚。
  实测（窗口 1244×880）：宿主 242px ⇒ **9 档一次看全**，主表也仍是 9/9。
- **百分比显示错了两位**（批 A2 现场探针抓到）：`.NET` 的 `P` 格式**自己会乘 100** ⇒ 先乘一次再套 `P0` 会得到 `5,095 %` 而不是 `51%`，
  且它还会在数字与 `%` 间插空格。现在**手拼 `%`**（`ScaleFit.Percent1`），与文档里的 `裁 −13.4%（左右各 −129px）` 逐字一致。
- **选型表：点最后一行、高亮却落在倒数第三行**（2026-09-20 用户实测）。**根因**：主视图的行高是按可用高度算的，
  但只改列表**自称**的行高（`fixedItemHeight`）**不会重排已经建出来的行**——实测声明值 `26`、九个行元素
  **仍是创建当刻的 `20px`**，于是"y ⇒ 第几行"的折算按 26 走、屏幕像素按 20 排，**越靠底部错得越多**
  （点第 8 行被判成第 6 行）。**修法**：改完行高让列表重建一次，声明值与渲染值重新一致。
  - **顺带修好一件更早的事**：正因为行元素一直没被重排，上一版那个「行高会自己铺开、不留一大片空白」
    （2026-09-19）**其实从未生效**——界面上的行一直是 20px。现在真的会铺开了。
- **工具栏 `match` 标签被挤成 3.3px、文字叠在滑块上**，且窗口底部多出一条空列表。**根因**：`.uxml` 里混进了
  **两个空的 `ListView`**——一个在那个 `Label` **里面**（把它挤成 3.3px 宽，文字溢出画到滑块上），
  一个在 `UXML` 根节点下（在窗口底部占掉一整条 1244×20）。两个都已删除。
- **`match` 标签措辞改准**：`match(宽-高)` → **`match（0=宽 · 1=高）`**（原写法容易被读成"宽减高"；
  `CanvasScaler.matchWidthOrHeight` 的语义是 **0 = 按宽度匹配、1 = 按高度匹配**）。

- **已知限制（如实登记，非缺陷）**：包内 `Tests/EditMode/` 的若干断言假设**"包根 + 仓库布局"**（数 `Samples~/` 目录与场景数、扫包内 `.cs`/`.asmdef`、卡实现文件行数），
  **拿到消费者工程里跑会失败**——那不代表你的工程有问题。README「已知限制」已补第 17 条说明（含成因：`Samples~` 这类以 `~` 结尾的目录 Unity 不导入 AssetDatabase，断言直接按路径读盘）。
- **导出的 CSV 中文乱码**：落盘改为 **UTF-8 带 BOM**。此前无 BOM，中文 Windows 的 Excel 按 GBK 猜码，全表中文乱码（用户 2026-09-18 实测截图）。
- **证据包表头自述的行数**：原来写"7 档 × … = 126 条"（实际通常更多，且会随现场条件变化）⇒ 改为由 `BuildCsv(out int dataRows)` 用**与 `ScaleTable.Build` 同一判据**算出的**实际**行数，`MaxDataRows`（127）作上限，并有断言逐行数过。
- **现场对账失败的提示**：活动场景未保存（无路径）导致 additive 临时场景建不起来时，错误文本追加可操作提示（`E-17`）；该失败本身**不是错误**——真值照标"未取到"，证据包降级为全离线列照常导出。
- **选型台列宽与列定义**（`W-ScaleCalc-23`）：行标识拆成 `屏幕档位` + `屏幕尺寸` 两列（与证据包 CSV 的 `屏幕档位,屏幕尺寸` 同构，此前一格塞不下最长档位名会被截断）；列宽不再写死，改为**按内容实测推默认宽 + 容器变化时按内容比例分摊**（新增 `Editor/ScaleTableColumns`，纯函数 + 可注入测量器）。
- **工具栏输入框**：`宽`/`高` 改成独立 `Label` + 无 label 的 `FloatField`（`BaseField` 的 label 带编辑器默认 `min-width: 120px`，原 140px 字段里输入框只剩约 20px，`1920` 显示不全），并开启 `isDelayed`——此前每敲一个字符就重算一次全表（输入 `1920` 会算 `1`/`19`/`192`/`1920` 四次），中间值还会被当成合法参考分辨率算进表。
- **界面里漏出的 markdown 星号**：分支状态栏与资源定位提示中写进 `Label` 的 `**`（UI Toolkit 不渲染 markdown）已去掉，改用 `「」`。

### 重构

- **`ScaleFit` 几何内核下沉到 `Editor/Table/Fit/`**（2026-09-20）：`Editor/Table` 曾到 **17 项 = 平台平铺上限**（12+5），
  现把 8 个 `ScaleFit.*`（几何 · 文本 · 统计 · 文案 · 排序筛选 · 字符条 · 详情 · 示意图）搬进**子目录 `Fit/`** ⇒ `Table` **9 项** · `Fit` **8 项**。
  - **对你的工程没有影响**：类型名、命名空间（`UGams.ScaleCalc.Editor`）、程序集、`.uxml`/`.uss` 内容全都没动，
    `.meta` 与 GUID **随文件成对搬走**（`git mv`）⇒ 你的场景 / 预制体引用一个都不断。
  - 有影响的一种情况：按旧路径 grep 本包源码 —— 把 `Editor/Table/ScaleFit.X.cs` 换成 `Editor/Table/Fit/ScaleFit.X.cs`。
- **测试文件改成"一事一类"**：`ScaleCalcBranchAndContractTests.cs`（一个文件装 3 个测试类）拆成
  `ScaleCalcWorldSpaceTests.cs` / `ScaleCalcEarlyExitTests.cs` / `ScaleCalcInputContractTests.cs`；
  `ScaleCalcClampBranchTests.cs`（2 个类）保留本体 + 分出 `ScaleCalcFiniteInputTests.cs`。
  **类名一字未动**（只是各回各家）⇒ 你按类名筛选测试的写法不受影响；变的是**文件路径**。
  新增机械判据 `EveryTestFile_HoldsExactlyOneTopLevelType_MatchingItsFileName`（文件名必须 = 该文件唯一的顶层类型名），
  这条口径此后由断言守着，不再靠人记。
- **包内目录整顿**（2026-09-20，平台风险项 `R-P2` 落地）：`Editor/` 从 **28 项**平铺、`Tests/EditMode/` 从 **32 项**平铺，
  按职责下沉到子目录——`Editor/`：`Window` · `UI` · `Table` · `Tiers` · `Evidence` · `Reconcile`；
  `Tests/EditMode/`：`Kernel` · `Probe` · `Table` · `Tiers` · `Evidence` · `Window` · `Reconcile` · `Package` · `Support`。
  每个目录回到平台口径 **12±5** 项内。
  - **对你的工程没有影响**：类型名、命名空间、程序集、菜单项、`.uxml`/`.uss` 的**内容**全都没动；
    `.meta` 与 GUID **成对搬走** ⇒ 你的场景 / 预制体 / asmdef 引用**一个都不断**，重新导入后照常可用。
  - 唯一变化是包内文件的**相对路径**：`.uxml`/`.uss` 现在住在 `Editor/UI/`。包内定位读的是
    `ScaleCalcAssets`（`UiFolder`），**你不需要写任何路径**。
  - 有影响的一种情况：你自己**复制过**包内路径，或按旧路径 grep 本包源码 —— 把 `Editor/` 下那一级换成上表里的子目录名即可。

### 变更

- **主视图行高会自己铺开**（2026-09-19 用户："太空了"）：条目只有 7 条而主视图有 400 多 px，
  原来固定 20px 行高会在表格**内部**留一大片空白。现在按可用高度摊（新类 `ScaleTableRowHeight`，
  纯函数 `For(itemCount, availableHeight)`），**夹在 20~26px**：够高就铺满、很高就封顶（余量留白）、
  挤的时候不低于 20（宁可让列表自己滚，也不破坏"内置 7 档一次看全"）。
  `MultiColumnListView` 没有"按容器自动撑行"的开关（实测枚举只有 `FixedHeight`/`DynamicHeight`），
  所以是自己在 `GeometryChangedEvent` 里算 `fixedItemHeight`。
  实测（窗口 1182×900）：`table-host` 403px · 7 行 · `fixedItemHeight` **26.0**（= 期望值，差 0.000）。
- **窗口底线 680 的来历被算清楚了**（并写明**不要往下调**）：实测反推固定段 **496** + 主表 **184**
  （表头 24 + 7 行 × 20）= **680**，正好卡在"7 档一次看全"上；实测算过 660 会逼出内部滚动条。
  主表 `min-height` 由 60 → **184**（与底线同一个算式的两头）。

- 🔴 **现场清场的副作用治理**（**副作用所有权原则**）：本作品会被挂进**别人的工程**，
  原来的清场逻辑有两条**会动到使用方东西**的路径，现已彻底移除：
  - **不再按名字删对象**。原实现对全工程扫 `__ScaleCalc_*` 并 `DestroyImmediate`（软标识认领归属，不可撤销）。
    现在**一个对象都不删**——清场完全交给 `CloseScene(自己建的临时场景)` 这一个原子动作；
  - **不再关闭不是自己建的场景**。原实现遍历所有已加载场景、把"未保存"的关掉（实测：对"未命名 + 有内容"的场景调
    `CloseScene(s, true)` 会**直接关闭、不弹框、不报错**）。现在收尾**只关创建当刻记下的那个场景句柄**，
    其余未保存场景**只报告、不动手**（诊断会点名是哪个场景挡路）；
  - **补回活动场景恢复**：建完临时场景后 `SetActiveScene(宿主)`（此前协议要求却没实现）——否则使用方在窗口开着时
    新建的对象会落进那个马上要被销毁的临时场景；
  - **硬前置**：宿主场景未保存（无路径）⇒ **直接拒绝**，且**零副作用**（不建、不删、不关任何东西）；
    **宿主只是"脏"不拒绝**（编辑器里改过东西宿主就是脏的），改由收尾**场景指纹**一比一复原并写进诊断；
  - 命名前缀 `__ScaleCalc_` **退出判据**（`Tag` 常量已删，对象改名为中性的 `ReconcileTemp`）；临时对象带
    `HideFlags.DontSave`，万一崩溃也不会被使用方存进自己的场景。

- 🔴 **向使用方工程写文件之前必须先有"明确的落点"**（**副作用所有权原则**）：
  本作品会被挂进**别人的工程**，原先的导出链路有三条**会动到使用方磁盘**的路径，现已收紧：
  - **不再自行选址**：删掉无参 `EvidencePackCommand.Export()`（它就是"无确认直写仓库约定路径"的入口）。
    程序化入口只剩 `ExportTo(path)`，**路径必须显式给**；
  - **不再造目录树**：删掉 `Directory.CreateDirectory`。目标父目录不存在 ⇒ 抛 `DirectoryNotFoundException`，
    **磁盘上不出现任何新目录**（原先一次 `Export()` 就能在人家工程根下造出 `Document/Works/ScaleCalc/Evidence/`，连 `Document/` 都可能是新造的）；
  - **不再静默覆盖**：`EvidenceExporter.Export` 增加 `allowOverwrite` 开关，**默认不覆盖**——目标文件已存在而未获许可
    ⇒ 抛 `IOException`，**文件逐字节未动**。窗口那两条导出路径显式传 `true`（依据是保存对话框的"替换吗？"，
    只有用户确认过才会走到写盘）；
  - `DefaultPath` **降级为窗口保存对话框的初值**（不再是任何"包内自选落点"的行为依据）；宿主没有 `Document/` 时
    **优雅退化为"只在工程根给一个文件名"**，不拼那截仓库目录、也不建它。
  - `EvidencePackCommand` 按章节拆 `partial`（`EvidencePackCommand.Csv.cs` 收 CSV 产出章节），两个文件都在 200 行红线内。
  - **新增 6 条断言**：不建目录（跑前跑后目录集合相同）· 无目录部分也拒绝 · 拒绝覆盖且文件未变 · 显式许可**确实**覆盖 ·
    源码级"无自行选址入口"判据 · 拆分后两文件都在红线内。

- 🔴 **"会动场景"这件事现在只说真话，而且只在你要的时候做**（**副作用所有权原则**）：
  原先那个公开的 `Build()` 名字与签名都像**纯函数**，内部却在建/拆临时场景；选型台更是**拖一次滑块就建拆几十次场景**、
  **打开窗口就建拆一次**。现已彻底分开：
  - **纯函数归纯函数**：`Build()` 删除，改为 **`BuildCsv(...)`** —— 真值由**调用方当参数**喂进来，
    它**不取真值、不碰场景**（断言：跑一次前后场景集合与宿主根对象数逐项不变）；
  - **现场对账有唯一显式入口**：新增 `EvidencePackCommand.MeasureLive(...)`——**名字就说出了副作用**
    （会建/拆临时场景），XML 注释第一句同样写明；全包**只有它**调对账协议（断言守住）；
  - **意图驱动**（选型台）：新增「现场对账」按钮（唯一会动场景的按钮）与「自动对账」开关（**默认关**）。
    **开窗不再对账**；不开自动对账时**拖滑块产生的对账次数 = 0**（只重算离线表）；打开自动对账则**停手 0.4 秒**才
    对账一次（防抖 ⇒ 拖一次滑块最多 1 次，`J5`）；
  - **导出零副作用**（`J9`）：窗口把对账结果**缓存**起来（`LiveTruth` + 可得性 + "哪一次取的"），导出按钮把这份缓存
    喂给纯函数 ⇒ 同一次会话连点导出，**场景操作次数 = 0**；没对过账导出就如实写「（离线列）」并在状态栏说明，**不假装**；
  - **状态栏如实报来历**（`B-5`）：写出"第 N 次现场对账（条件 …）· 测量条件 … · 渲染尺寸 …（源）"，未对账则明说"未取到"，
    不写安慰性措辞；未对账时分支判定显示「未对账」而不是含糊的"未取到真值"。
  - 窗口按职责拆 `partial`：`ScaleCalcWindow.cs`（主体 152 行）· `.Measure.cs`（对账章节 110 行，**唯一动场景的地方**）·
    `.Status.cs`（状态与呈现 79 行）· `.Export.cs`（导出 72 行）——原文件 186 行已逼近红线。
  - **新增 6 条断言**：离线入口不动任何场景（两条）· 重算路径不出现测量调用 · 现场入口唯一且注释写明会建/拆场景 ·
    导出路径只吃缓存真值 · 现场对账只经按钮/防抖到达且开关默认关。

- **删掉死守卫 `m_IgnoreFieldChange`**：它从未被置为 `true`（"防重入重建"的假守卫），`Rebuild()` 顶部改为说明性注释——本窗口**不做程序化写字段**，故不需要重入守卫；并注明将来若程序化写 `FloatField`/`Slider` 的 `value` 必须同时加回。
- **撤掉未接线的「逐行重算计时」开关**：它的 `Toggle` 值从未被任何逻辑读过（勾上只多触发一次全表重算），标签却在承诺"逐行计时"。按用户口径**删掉**（控件 + 字段 + 回调 + 无用 USS 规则），**不是**补实现——真要逐行耗时时再带着"要回答"超过 16ms 才值得做增量重算"这个前提加回来。
- **证据包的真值只在一个块里**：现场只对账一次（参考 `1920×1080` × `match 0.5`），**只有那一块**带现场真值，其余块整表标「（离线列）」并在文件头写明原因。此前把一次测得的真值写进全部 6 组参考块 ⇒ 别的块出现「两组不同参考分辨率相减」的**假差值**（实测 `1280×720` 块 `Δsf=0.667`）。测量条件现在随真值一起带出（参考分辨率 / `match`），由 `ScaleTable.Build` 守门。
- `ExpectedDataRows`（144）→ **`MaxDataRows`**（= 6 × 3 × 7 + 1 = 127，上限）；顺带消掉两处硬编码 `0.5f`（`match - 0.5f` 与模板里的）——现在只有一个 `LiveMatch`。

### 移除

- 🔴 **物理覆盖表整条撤除**（设计册 `W-ScaleCalc-10` 重做，2026-09-19）：选型台里那张 40 行
  「物理单位 × dpi 档位」折叠表、它的列定义、它的 CSV（`EvidenceExporter.BuildPhysicsCsv`）、
  以及它那 8 条断言**一并删除**。
  - **为什么**：5 个物理单位只是同一算式的 5 个除数；8 个 dpi 档位里 **6 个是硬编码常量**；
    `referencePixelsPerUnit` 那列在全部档位上**恒定**（dpi 不进这个式子）。⇒ **40 行里 35 行
    永远停在"未实测"**，与唯一一行真值并排陈列，只会让人以为"这个包覆盖了 8 种机型"——
    而它其实一个机型都没覆盖，只是在把同一个除法念 8 遍。
  - **替代**：**兜底链诊断**（见下条）。本作品的产出面上**不再有一个数字是猜的**。
  - **你需要档位换算表时**：直接 `dpi / targetDPI`（`targetDPI`：厘米 2.54 · 毫米 25.4 ·
    英寸 1 · 磅 72 · 派卡 6）。本作品不再替你列那张表。
  - ⚠️ **破坏性**：`EvidenceExporter.BuildPhysicsCsv` 已删；界面上的「物理覆盖表」折叠区已删。

### 测试

- 新增 `ScaleCalcTableColumnTests`（10 条）：列定义（7 列、行标识拆两列）· 列宽按内容推（最长行决定、内容翻倍则变宽、测量失败不塌成 0、`NaN` 不污染）· `Distribute` 把剩余宽度摊给**所有**列（"一列很宽"的回归断言）、挤不下到底线、未布局时退回内容宽。
- 新增 3 条导出断言：`Export_WritesUtf8Bom_SoExcelDoesNotMojibake`（首三字节 `EF BB BF`）· `EvidencePack_HeaderRowCount_MatchesActualDataRows`（表头自称行数 = 逐行数出来的实际行数）· `EvidencePack_TruthOnlyInTheLiveBlock`（回归：非现场参考分辨率的块一律不得出现现场来源）。

## [0.1.0] - 2026-09-17

首个版本。内核 + 适配层 + 常驻探针 + 选型台 + 取样例 + 证据包，`0.1.0` 阶段允许破坏性变更。

### 新增

- 作品包骨架（`package.json` · `Runtime/Core` · `Runtime/Unity` · `Editor` · `Tests/EditMode` · `Samples~`）。
- 零依赖内核 `UGams.ScaleCalc`：`ScaleCalc.Evaluate` 复刻 `CanvasScaler` 三种 `ScaleMode` 与两处早退的换算链（含三处夹取、两道写回门限常量、诊断位）与手算常量表 `HandCalcSamples`。零 Unity 依赖由 `noEngineReferences: true` **强制**（不是靠纪律）。
- 适配层 `UGams.ScaleCalc.Unity`：`ScaleEnumMap`（引擎枚举 ↔ 内核枚举，双向恒等）· `ScaleInputCollector`（含屏幕尺寸源 `ScreenSizeSource`）。
- 常驻探针 `ScaleFactorProbe`：采样时机状态机、稳定帧三条件（含 `enabled` 检查防"假性稳定"）、两个私有 `m_Prev*` 字段**各自独立降级**、变化驱动的文本输出；`Capture()` 零分配（1000 次采样实测内存无增长）。
- `ProbeReport`（结构化，可断言）/ `ProbeVerdict`（五档判定，优先级短路）/ `ProbeText`（一行一件事的文本层）。
- 选型台窗口 `UGams/ScaleCalc/打开选型台`（UI Toolkit）：`MultiColumnListView` 表格（行 = 屏幕档位，列 = 三种 `ScaleMode` + 溢出/留白方向 + 差值）、分支状态栏（显式画出两处早退）、物理尺寸覆盖表（三参数 × dpi 档位 + 手填/本机实测/未实测三态）、证据导出（CSV，四列 + 输入源）。
- `Samples~`：三模式对照示例（三个场景）+ 早退反例（子 `Canvas` 缩放器不生效 / `WorldSpace` 走 `dynamicPixelsPerUnit`）。
- 换算证据包生成器（菜单 `UGams/ScaleCalc/导出换算证据包`）：6 组参考分辨率 × 8 行 × 3 个 `match` = 144 条记录，一条命令可重新生成。
- **78 条 EditMode 断言**：内核手算对账（含 `A-3` 反例：`match = 0.5` 必须是对数平均 `1.0000`，算术平均会给 `1.25`）· 三道写回门限 · 三处夹取 · 恒有限 · 判定优先级 · 表格真值可得性 · 物理覆盖表 · 证据导出 · 包卫生 · 可脱离宿主的静态前提。

### 变更

- `package.json` 的 `dependencies` 补 `com.unity.test-framework`：`Tests` 程序集显式引用 `UnityEngine.TestRunner` / `UnityEditor.TestRunner`，不声明会让**消费方**的测试程序集引用悬空。
- `Tests` 的 asmdef **去掉** `defineConstraints: ["UNITY_INCLUDE_TESTS"]`：该宏在 live 编辑器通道下不被定义 ⇒ 测试程序集永不编译、`run_tests` 恒报"0 条全过"。

### 已知限制

见 `README.md` §四.3（16 条，逐条对应设计口径）。两条与本版本关系最紧的：`Screen.dpi` 在编辑器/桌面上不可信（`ConstantPhysicalSize` 结论标未实测）；探针的采样循环不做自动化验收（平台验收优先序不把 Play 当手段）。
