# Changelog

本文件记录 `com.wayward.scalecalc` 的版本变更。版本号遵循 [SemVer](https://semver.org/lang/zh-CN/)，格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
版本号与 `package.json` 的 `version` 一致，发布以 git tag 标记（本作品：`scalecalc/v<版本>`）。

> 只写**对使用者的影响**，每条一行；逐条开发细节留在开发仓库的《文档变更记录》与各册设计 / 实现文档里。
> ℹ️ `0.4.0` 之前的条目用的是旧名（`com.ugams.scalecalc` / `UGams.*` / `UGams/` 菜单前缀）——那是当时的真实状态，按留痕原则保持原文不改。

## [Unreleased]

_暂无。_

## [0.4.0] - 2026-09-22

> 改名版，属破坏性变更：`ugams` 是立项时的占位名，本版起统一为 `wayward`。

### 变更

- 包名：`com.ugams.scalecalc` → `com.wayward.scalecalc`。装过旧版的工程要同步改 `Packages/manifest.json` 里的依赖键，否则解析失败或出现"两个包"。
- 程序集与命名空间：`UGams.ScaleCalc[.Unity|.Editor|.Tests]` → `Wayward.ScaleCalc[...]`。`.meta` 与 GUID 成对改名未动 ⇒ 场景与预制体的组件引用不断；用旧命名空间 `using` 的地方会报 CS0246。
- 编辑器菜单：`UGams/ScaleCalc/打开选型台` → `Wayward/ScaleCalc/打开选型台`。
- `EditorPrefs` 键前缀：`UGams.ScaleCalc.ScreenTiers.*` → `Wayward.ScaleCalc.ScreenTiers.*`。**症状**：升级后打开选型台读不到原先存过的屏幕档位工作集，回落内置初值；**回退**：旧键仍在注册表里，改回旧名即可读回。
- 源码里的包路径字面量、包内测试的命名空间白名单与禁词表、README 安装地址、档位清单文件首行注释、证据导出 CSV 表头里的包名，同步更新。

### 移除

- **`Samples~` 与 `package.json` 的 `samples` 段**（`Q-16`：作品默认不给示例，只有作者明确要求时才加）。原先带两组示例：三模式对照（3 个场景）与早退反例（1 个）。
  **症状**：升级后 Package Manager 里本包的 Samples 区为空；**根因**：默认不带示例；**回退**：装回 `v0.3.0`，或从旧版包里把 `Samples~/` 拷进自己的工程（示例与包代码无耦合）。

### 修复

- 改名残留收口：证据册来源行、`Samples~` 场景里缓存的旧类型标识、平台文档里残留的旧名。证据册正文与历史条目里的旧名按留痕原则保留。

### 文档

- `README` 重写为告知页：一句定位 + 三条客观数据支持（多设备数据推演 · 安全设计区计算 · 明确边界）；
  §一 三模块（独立换算内核 · 引擎对账验证 · 编辑器可视化窗口）· §二 只留要求与 Git URL · §三 三类信息（结论区 · 档位管理表 · 选型表）· §四 只留许可证；英文摘要与中文同口径。
- 本文件按同一文风精简（**426 → 102 行**），逐条细节移出到开发仓库的《文档变更记录》。

## [0.3.0] - 2026-09-22

> 首个公开开源版本。

### 新增

- **对账状态列**（表格第 1 列）：`失` / `过` / `—` 三态——只有"当前渲染尺寸"那一行可能判出结果，其余行如实写 `—`，悬停说明原因（四种成因互不相同）。
- **逐档裁留示意图列**（表格第 2 列）：参考画布框 + 本档画布框同比例尺并排，红 = 被裁、黄 = 留白，一格里能同时看见裁与留。

### 变更

- 推荐 `match` 行由两件扩到**五件**：目标函数 · 会裁 X/N · 最坏裁切 · 留白代价 · 安全设计区。
- 术语统一：三方式小结里的「安全区」改为全称「**安全设计区**」。
- 列集 8 → 9：行首插入「对账」列，排序键映射整体后移。

### 移除

- 「更多列」开关整条链路（实测那三列里 `分支` 整表同值、`夹取诊断` 对内置档恒为"无"）。
- 窗口底部的分支栏与总结行（释放约 128px ⇒ 主表可见行 8 → 12）。**如实登记的损失**：界面上的分支可视化与兜底链诊断不再成立，补偿出口是对账列悬停、证据包 CSV 与本文档。
- 「选中行详情」面板（行高亮保留，选中态仍按档位名恢复）。

### 修复

- 🔴 **末列「裁留图」整列画不出来，Console 反复抛 `InvalidCastException`**：图格的绑定与清格判据改按"列的种类"分派，不再按子元素个数猜；并补 4 条逐列 × 逐行的真调用断言（此前没有任何判据碰过绑定路径，这类事故才能一路绿着上线）。

## [0.2.0] - 2026-09-20

> 两块用户可感的新功能：档位清单导入导出、选型台结论面。无破坏性变更。

### 新增

- **档位清单导入 / 导出**：一份可入库、可手改的 `.txt`（UTF-8 无 BOM + LF），内容就是本机存档的那份载荷；导入是整体替换，先解析、全过才动数据（坏文件零副作用）。团队想统一"关心哪些分辨率"，共享这个文件即可。
- **选型台结论面**：顶部结论条（常显）· 三方式对比小结 · 推荐 `match` 值（连同留白代价一起报）· 危险带常显 · 「复制结论」· 「导出设计约束单（几何口径）」。
- **可视化**：安全区示意图（外框 = 参考画布、内框 = 安全设计区、四边带 = 危险带）· 单元格比例底衬 · 字符条 · 「导出当前视图」（所见即所得，跟随当前排序与筛选）。
- **屏幕档位可维护化**：工作集 + 档位管理表（勾选 / 就地改名 / 增删自定义档；内置 7 档不可删）。工作集按工程存在编辑器配置里，不落仓库。
- **兜底链诊断**：本机读数与兜底常量两种来源分开说明，缺读数时只出后者。
- 表格口径图例：一行说明"灰字 = 内核离线算的，真值只对现场对账过的那一行成立"。

### 修复

- 导出的 CSV 中文乱码：落盘改 UTF-8 带 BOM（否则中文 Windows 的 Excel 按 GBK 猜码）。
- 证据包表头自述的行数改为按实际算；真值只在与现场同条件的块里有效。
- 选型表点最后一行、高亮却落在倒数第三行：改完行高要让列表重建一次。
- 百分比显示多乘了一次 100；工具栏输入框被标签挤到只剩约 20px（改为独立标签 + `isDelayed`）。
- 窗口底线抬到 900×950 并写明不要再往下调。

### 重构

- 几何内核下沉到 `Editor/Table/Fit/`；包内目录按职责下沉，`Editor/` 与 `Tests/EditMode/` 都回到 12±5 项（类型名、命名空间、`.uxml`/`.uss` 内容未动，`.meta` 与 GUID 随文件搬走）。
- 测试文件"一事一类"：文件名 = 该文件唯一的顶层类型，并加机械判据守住。

## [0.1.0] - 2026-09-17

首个版本：零依赖内核（三种 `ScaleMode` 与两处早退的完整换算链，由 `noEngineReferences` 强制）· 适配层 · 常驻探针（`Capture()` 零分配）· 选型台窗口 · 换算证据包 · **78 条 EditMode 断言**。

### 变更

- `package.json` 补 `com.unity.test-framework`：测试程序集引用了 `UnityEngine.TestRunner` / `UnityEditor.TestRunner`，不声明会让**消费方**编译不过。
- 测试 asmdef 去掉 `defineConstraints: ["UNITY_INCLUDE_TESTS"]`：该宏在 live 编辑器通道下不被定义 ⇒ 测试程序集永不编译、跑测试恒报"0 条全过"。

### 已知限制

见开发仓库的《ScaleCalc 设计要点》§五（含"`Screen.dpi` 在编辑器/桌面不可信"与"探针的采样循环不做自动化验收"两条）。
