namespace Wayward.ScaleCalc
{
    /// <summary>画布渲染模式。**数值与 <c>UnityEngine.RenderMode</c> 一致**。</summary>
    public enum CanvasRenderMode
    {
        ScreenSpaceOverlay = 0,
        ScreenSpaceCamera = 1,
        WorldSpace = 2,
    }
}
