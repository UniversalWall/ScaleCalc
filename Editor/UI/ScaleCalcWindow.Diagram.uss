/* ScaleCalc 选型台 · **逐档裁留示意图列**的样式
   拆的判据 = 200 行红线 + 职责不同：`ScaleCalcWindow.uss` 管"整窗骨架与两张表"，本文件只管**图这一列**。
   与 `ScaleCalcWindow.Verdict.uss`（结论区那一片 + 比例底衬的配色）同一套拆法。

   🔴 **现行画法 =「同一比例尺 + 双色带」**（它替换掉的那一版被现场否掉：「这也太丑了，
   不能很好的展现留白和裁剪」）。要点：
   · 坐标系 = **信封**（参考框与画布框的逐轴并集）⇒ 两框**同一比例尺**、都落在信封内 ⇒ 图**永不出血**；
     宽高比由装配层按 `EnvelopeAspect` 给的**像素尺寸**保证（**绝不拉伸**）；
   · 两框的**差**画成色带：**裁 = 红**（参考有、画布没有）· **留 = 黄**（画布有、参考没有）
     ⇒ 裁与留**同格同时可见**（初版一格只有一种颜色，`手机竖屏 9:16` 的"左右裁 + 上下留"里留白全被吃掉）；
   · 颜色只是**第三重编码**，悬停文本才是主说明（红黄含义 + 两轴读数 + 限定语都在里面）。 */

/* 宿主格：信封在格内横竖居中——**居中由宿主 flex 给出**（装配层不写 px 偏移 ⇒ 列宽被拖动也不失准）。 */
.cell-diagram {
    /* 刻意不写 overflow：元素都在信封内，本列不再依赖"允许溢出"（那正是初版出血到邻行的来源）。 */
}

/* 作画区 = **信封**：像素宽高由装配层按 `EnvelopeAspect` 拟合（行高 − 呼吸位）。
   ⚠️ 这里**不写尺寸**（否则与装配层的 inline 样式打架）；它同时是六个子元素的**定位祖**——
   百分比按**父节点**解析，子元素挂错父节点就会按整格算（实测踩过）。 */
.cell-diagram-area {
    /* 尺寸交给装配层；本规则只作"这个类存在"的锚点与说明。 */
}

/* 四条差带：**实色**（22px 高的小图里颜色就是主编码；红黄的含义写在悬停里）。 */
.cell-diagram-band {
    background-color: rgb(120, 120, 120);
}
.cell-diagram-band.cell-diagram-crop {
    background-color: rgb(170, 90, 90);
}
.cell-diagram-band.cell-diagram-spare {
    background-color: rgb(180, 155, 70);
}

/* 参考画布框：**只有描边**（灰）——它是"尺子"，不是"内容"，所以不上底色。 */
.cell-diagram-ref {
    border-width: 1px;
    border-color: rgb(150, 150, 150);
}

/* 本档画布框 = **实心块**（可读性优化：原来是"浅描边 + 极淡底衬"，22px 的小图里
   两块描边糊成一团 ⇒ 现在"**蓝块 = 我这块画布**、灰框 = 参考画布"一眼分得开）。
   ⚠️ 它画在**差带之下**（见装配层的元素顺序）："留白"的黄段落在块内，必须压在块上面才看得见。 */
.cell-diagram-canvas {
    background-color: rgb(92, 122, 162);
    border-width: 1px;
    border-color: rgb(122, 156, 196);
}

/* 🔴 **本列格内没有文字**（行高 20~30px 装不下长读数 ⇒ 读数只走悬停）。
   所以这里**没有** `.cell-diagram-text` 规则——曾有过，随文字一起删（`.uss` 规则必须与元素成对，
   否则就是"删了节点没删规则"）。 */
